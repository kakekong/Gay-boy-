"""AI layer.

Each submodule encapsulates one capability described in docs/06-ai-logic-design.md.
They are intentionally split between deterministic logic (sklearn, rules) and
LLM-backed logic (assistant, quotation compose, doc parse) for testability.
"""
